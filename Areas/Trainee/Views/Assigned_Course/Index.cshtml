@model IEnumerable<Asm.Models.Entity.Assigned_Course>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Trainee/Views/Shared/_MyLayout.cshtml";
}

<h2>Asigned Course</h2>

<br />
<br />

<ul style="list-style:none;display:inline-block;">
    @foreach (var item in Model)
    {
        <li style="display:inline-block; margin-right:80px;">
            <div class="card" style=" border-color:lightgray; border-style: solid; border-width:1px; width:345px; ">
                <img class="card-img-top" src="https://www.fpt.com.vn/Content/home/images/img/fptf4.jpg" style="height:200px">
                <div class="card-body" style="text-align:center; margin-top:20px">
                    <h4 class="card-title" style="font-weight:bold; ">@Html.DisplayFor(modelItem => item.Course.CourseName)</h4>
                    <p class="card-text" style="margin-top:20px; margin-bottom:10px">Trainer: @ViewBag.Name</p>
                </div>
                <button type="button" class="btn btn-primary" style="margin-left:100px"><a href="@Url.Action("Details", "Assigned_Course", new { id = item.CourseID })" style="color:white">View Details</a></button>
                <br />
                <br />
            </div>
        </li>
    }
</ul>